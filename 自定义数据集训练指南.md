# Ultralytics YOLO 自定义数据集训练指南

本指南将详细介绍如何使用 Ultralytics YOLO 训练您自己的数据集。

## 目录

1. [项目简介](#项目简介)
2. [环境安装](#环境安装)
3. [数据集准备](#数据集准备)
4. [配置文件说明](#配置文件说明)
5. [训练模型](#训练模型)
6. [验证和测试](#验证和测试)
7. [常见问题](#常见问题)

## 项目简介

Ultralytics YOLO 是一个强大的目标检测、实例分割、图像分类和姿态估计框架。它支持多种任务类型：

- **目标检测 (Detection)**: 检测图像中的物体并用边界框标注
- **实例分割 (Segmentation)**: 不仅检测物体，还分割出物体的精确轮廓
- **图像分类 (Classification)**: 对整张图像进行分类
- **姿态估计 (Pose Estimation)**: 检测人体或动物的关键点
- **定向边界框检测 (OBB)**: 检测旋转的物体

## 环境安装

### 方法一：使用 pip 安装（推荐）

```bash
# 安装 ultralytics 包
pip install ultralytics

# 或者安装开发版本
pip install git+https://github.com/ultralytics/ultralytics.git@main
```

### 方法二：使用 conda 安装

```bash
# 使用 conda 安装
conda install -c conda-forge ultralytics
```

### 方法三：从源码安装

```bash
# 克隆仓库
git clone https://github.com/ultralytics/ultralytics
cd ultralytics

# 以可编辑模式安装
pip install -e .
```

### 验证安装

```python
from ultralytics import YOLO
import torch

print(f"YOLO 已成功安装")
print(f"PyTorch 版本: {torch.__version__}")
print(f"CUDA 可用: {torch.cuda.is_available()}")
```

## 数据集准备

### 目标检测数据集格式

对于目标检测任务，YOLO 使用以下目录结构：

```
my_dataset/
├── images/
│   ├── train/
│   │   ├── image1.jpg
│   │   ├── image2.jpg
│   │   └── ...
│   └── val/
│       ├── image1.jpg
│       ├── image2.jpg
│       └── ...
└── labels/
    ├── train/
    │   ├── image1.txt
    │   ├── image2.txt
    │   └── ...
    └── val/
        ├── image1.txt
        ├── image2.txt
        └── ...
```

### 标注文件格式

每个图像对应一个 `.txt` 标注文件，格式如下：

```
<class_id> <x_center> <y_center> <width> <height>
```

- `class_id`: 类别索引（从 0 开始）
- `x_center`, `y_center`: 边界框中心点坐标（归一化到 0-1）
- `width`, `height`: 边界框宽度和高度（归一化到 0-1）

**示例：**

```
0 0.5 0.5 0.3 0.4
1 0.7 0.3 0.2 0.2
```

### 实例分割数据集格式

对于实例分割，标注文件格式如下：

```
<class_id> <x1> <y1> <x2> <y2> <x3> <y3> ...
```

每行包含一个对象的类别和多边形坐标点（归一化）。

### 图像分类数据集格式

对于分类任务，使用以下目录结构：

```
my_classification_dataset/
├── train/
│   ├── class1/
│   │   ├── image1.jpg
│   │   └── image2.jpg
│   └── class2/
│       ├── image1.jpg
│       └── image2.jpg
└── val/
    ├── class1/
    │   └── image1.jpg
    └── class2/
        └── image1.jpg
```

## 配置文件说明

创建一个 YAML 配置文件来描述您的数据集。

### 目标检测配置示例

创建 `my_dataset.yaml` 文件：

```yaml
# 数据集根目录路径
path: /path/to/my_dataset  # 数据集根目录（绝对路径或相对路径）

# 训练集、验证集和测试集路径（相对于 path）
train: images/train  # 训练集图像目录
val: images/val      # 验证集图像目录
test:                # 测试集图像目录（可选）

# 类别名称
names:
  0: person
  1: car
  2: dog
  3: cat

# 类别数量（可选，会自动从 names 计算）
nc: 4
```

### 实例分割配置示例

```yaml
path: /path/to/my_segmentation_dataset
train: images/train
val: images/val

names:
  0: person
  1: car
  2: bicycle
```

### 图像分类配置示例

```yaml
path: /path/to/my_classification_dataset
train: train
val: val

names:
  0: cat
  1: dog
  2: bird
```

## 训练模型

### 使用 Python API 训练

#### 目标检测训练

```python
from ultralytics import YOLO

# 加载预训练模型（推荐）
model = YOLO("yolo11n.pt")  # nano 模型（最快）
# model = YOLO("yolo11s.pt")  # small 模型
# model = YOLO("yolo11m.pt")  # medium 模型
# model = YOLO("yolo11l.pt")  # large 模型
# model = YOLO("yolo11x.pt")  # xlarge 模型（最准确）

# 训练模型
results = model.train(
    data="my_dataset.yaml",  # 数据集配置文件
    epochs=100,              # 训练轮数
    imgsz=640,               # 图像尺寸
    batch=16,                # 批次大小
    name="my_model",         # 实验名称
    device=0,                # GPU 设备（0 表示第一块 GPU，'cpu' 表示 CPU）
    workers=8,               # 数据加载线程数
    patience=50,             # 早停耐心值
    save=True,               # 保存检查点
    project="runs/detect",   # 项目文件夹
)
```

#### 实例分割训练

```python
from ultralytics import YOLO

# 加载分割模型
model = YOLO("yolo11n-seg.pt")

# 训练
results = model.train(
    data="my_segmentation_dataset.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
)
```

#### 图像分类训练

```python
from ultralytics import YOLO

# 加载分类模型
model = YOLO("yolo11n-cls.pt")

# 训练
results = model.train(
    data="my_classification_dataset.yaml",
    epochs=100,
    imgsz=224,
    batch=32,
)
```

### 使用命令行训练

#### 目标检测

```bash
# 基础训练
yolo detect train data=my_dataset.yaml model=yolo11n.pt epochs=100 imgsz=640

# 使用多 GPU 训练
yolo detect train data=my_dataset.yaml model=yolo11n.pt epochs=100 imgsz=640 device=0,1

# 自定义更多参数
yolo detect train data=my_dataset.yaml model=yolo11n.pt \
    epochs=100 imgsz=640 batch=16 \
    patience=50 save=True \
    project=runs/detect name=my_experiment
```

#### 实例分割

```bash
yolo segment train data=my_segmentation_dataset.yaml model=yolo11n-seg.pt epochs=100 imgsz=640
```

#### 图像分类

```bash
yolo classify train data=my_classification_dataset.yaml model=yolo11n-cls.pt epochs=100 imgsz=224
```

### 重要训练参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `epochs` | 100 | 训练轮数 |
| `imgsz` | 640 | 输入图像尺寸 |
| `batch` | 16 | 批次大小（根据 GPU 内存调整）|
| `device` | None | 训练设备（0=GPU 0，cpu=CPU，0,1=多GPU）|
| `workers` | 8 | 数据加载线程数 |
| `optimizer` | 'auto' | 优化器（SGD、Adam、AdamW 等）|
| `lr0` | 0.01 | 初始学习率 |
| `patience` | 100 | 早停耐心值（轮数）|
| `save` | True | 保存训练检查点 |
| `project` | 'runs/detect' | 项目保存路径 |
| `name` | 'exp' | 实验名称 |
| `exist_ok` | False | 覆盖现有实验 |
| `pretrained` | True | 使用预训练权重 |
| `resume` | False | 从上次检查点恢复训练 |

### 恢复训练

如果训练中断，可以从检查点恢复：

```python
from ultralytics import YOLO

# 从上次检查点恢复
model = YOLO("runs/detect/my_model/weights/last.pt")
results = model.train(resume=True)
```

或使用命令行：

```bash
yolo detect train resume model=runs/detect/my_model/weights/last.pt
```

## 验证和测试

### 验证模型

```python
from ultralytics import YOLO

# 加载训练好的模型
model = YOLO("runs/detect/my_model/weights/best.pt")

# 在验证集上评估
metrics = model.val()

# 打印指标
print(f"mAP50: {metrics.box.map50}")
print(f"mAP50-95: {metrics.box.map}")
```

使用命令行：

```bash
yolo detect val model=runs/detect/my_model/weights/best.pt data=my_dataset.yaml
```

### 预测/推理

#### 单张图像预测

```python
from ultralytics import YOLO

model = YOLO("runs/detect/my_model/weights/best.pt")

# 预测
results = model("path/to/image.jpg")

# 处理结果
for result in results:
    boxes = result.boxes  # 边界框
    probs = result.probs  # 类别概率（分类）
    
    # 显示结果
    result.show()
    
    # 保存结果
    result.save("result.jpg")
```

#### 批量预测

```python
# 预测文件夹中的所有图像
results = model("path/to/images/")

# 预测视频
results = model("path/to/video.mp4")

# 预测网络摄像头
results = model(0)  # 0 表示默认摄像头
```

#### 命令行预测

```bash
# 预测图像
yolo detect predict model=runs/detect/my_model/weights/best.pt source=image.jpg

# 预测文件夹
yolo detect predict model=runs/detect/my_model/weights/best.pt source=images/

# 预测视频
yolo detect predict model=runs/detect/my_model/weights/best.pt source=video.mp4

# 保存结果
yolo detect predict model=runs/detect/my_model/weights/best.pt source=image.jpg save=True
```

### 导出模型

将训练好的模型导出为其他格式：

```python
from ultralytics import YOLO

model = YOLO("runs/detect/my_model/weights/best.pt")

# 导出为 ONNX
model.export(format="onnx")

# 导出为 TensorRT
model.export(format="engine")

# 导出为 CoreML
model.export(format="coreml")
```

支持的导出格式：
- `onnx`: ONNX
- `torchscript`: TorchScript
- `engine`: TensorRT
- `coreml`: CoreML
- `tflite`: TensorFlow Lite
- `pb`: TensorFlow SavedModel
- `openvino`: OpenVINO

## 常见问题

### 1. 如何选择合适的模型？

- **YOLO11n**: 最快，适合实时应用，精度较低
- **YOLO11s**: 速度快，精度中等
- **YOLO11m**: 平衡速度和精度
- **YOLO11l**: 精度高，速度较慢
- **YOLO11x**: 最高精度，速度最慢

### 2. 如何处理数据不平衡？

使用类别权重：

```python
model.train(
    data="my_dataset.yaml",
    epochs=100,
    imgsz=640,
    # 其他参数...
)
```

或使用数据增强来平衡数据。

### 3. 如何减少过拟合？

- 使用数据增强
- 减少模型复杂度（使用更小的模型）
- 增加训练数据
- 使用正则化（dropout、weight decay）
- 提前停止训练（patience 参数）

### 4. 训练时内存不足怎么办？

- 减小 `batch` 大小
- 减小 `imgsz` 图像尺寸
- 使用更小的模型（如 yolo11n）
- 使用混合精度训练（自动启用）

### 5. 如何提高检测精度？

- 使用更大的模型（yolo11l 或 yolo11x）
- 增加训练轮数
- 使用更多训练数据
- 调整超参数（学习率、增强参数等）
- 确保标注质量高

### 6. 如何使用自己的数据增强？

```python
# 使用 Albumentations 库
model.train(
    data="my_dataset.yaml",
    epochs=100,
    hsv_h=0.015,      # 色调增强
    hsv_s=0.7,        # 饱和度增强
    hsv_v=0.4,        # 明度增强
    degrees=0.0,      # 旋转角度
    translate=0.1,    # 平移
    scale=0.5,        # 缩放
    shear=0.0,        # 剪切
    perspective=0.0,  # 透视变换
    flipud=0.0,       # 上下翻转概率
    fliplr=0.5,       # 左右翻转概率
    mosaic=1.0,       # 马赛克增强概率
    mixup=0.0,        # MixUp 增强概率
)
```

### 7. 如何使用多 GPU 训练？

```python
# Python
model.train(data="my_dataset.yaml", device=[0, 1, 2, 3])  # 使用 4 块 GPU
```

```bash
# 命令行
yolo detect train data=my_dataset.yaml device=0,1,2,3
```

### 8. 如何监控训练过程？

训练结果会保存在 `runs/detect/my_model/` 目录下：

- `weights/best.pt`: 最佳模型权重
- `weights/last.pt`: 最后一轮模型权重
- `results.png`: 训练曲线图
- `confusion_matrix.png`: 混淆矩阵
- `train_batch*.jpg`: 训练批次示例

可以使用 TensorBoard 或 Weights & Biases 进行可视化：

```python
# 使用 TensorBoard
# 训练时会自动记录
# 使用以下命令查看：tensorboard --logdir runs/detect

# 使用 Weights & Biases
model.train(data="my_dataset.yaml", project="wandb")
```

### 9. 从零开始训练还是使用预训练模型？

**强烈建议使用预训练模型**，因为：
- 收敛更快
- 需要的数据更少
- 通常能获得更好的性能

只有在非常特殊的场景下才从头训练。

### 10. 数据集太小怎么办？

如果数据集很小（少于 1000 张图像）：
- 使用预训练模型
- 使用强数据增强
- 减小模型复杂度
- 考虑使用迁移学习
- 寻找或生成更多数据

## 完整训练示例

```python
from ultralytics import YOLO

# 1. 加载预训练模型
model = YOLO("yolo11n.pt")

# 2. 训练模型
results = model.train(
    data="/path/to/my_dataset.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
    name="my_custom_model",
    device=0,
    patience=50,
    save=True,
    project="runs/detect",
)

# 3. 验证模型
metrics = model.val()
print(f"mAP50-95: {metrics.box.map}")
print(f"mAP50: {metrics.box.map50}")

# 4. 预测
results = model("test_image.jpg")
results[0].show()

# 5. 导出模型
model.export(format="onnx")
```

## 参考资源

- [官方文档](https://docs.ultralytics.com/)
- [GitHub 仓库](https://github.com/ultralytics/ultralytics)
- [数据集格式文档](https://docs.ultralytics.com/datasets/)
- [训练教程](https://docs.ultralytics.com/modes/train/)
- [Discord 社区](https://discord.com/invite/ultralytics)

## 结语

通过本指南，您应该能够成功地使用 Ultralytics YOLO 训练自己的数据集。如果遇到问题，请查阅[官方文档](https://docs.ultralytics.com/)或在 [GitHub Issues](https://github.com/ultralytics/ultralytics/issues) 中寻求帮助。

祝训练顺利！🚀
